# ── Frontend (baked into the JS bundle at build time by Vite) ──────────
# Set these in Heroku BEFORE the first deploy so they are available during
# the build step (heroku config:set VITE_SUPABASE_URL=... etc.)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ── Backend (runtime secrets — never exposed to the browser) ───────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ── Optional ───────────────────────────────────────────────────────────
# Heroku sets PORT automatically — do not set it manually on Heroku.
# PORT=3001

# If you want to restrict CORS in production to a specific origin:
# CLIENT_ORIGIN=https://your-app.herokuapp.com
